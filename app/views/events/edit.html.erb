<%= simple_form_for([@calendar, @event]) do |form| %>
  <%= form_error_hint(@event) %>
  <%= label_tag(:date) %>
  <%= text_field_tag(:date, date_from_day_number(params[:occurred_on]), disabled: true) %>
  <%= form.input(
    :occurred_on,
    input_html: { value: @event.occurred_on.day_number },
    as: :hidden
  ) %>
  <%= form.input(:title) %>
  <%= form.submit %>
<% end %>
