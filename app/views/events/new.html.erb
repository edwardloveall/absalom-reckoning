<%= simple_form_for([:calendar, @event]) do |form| %>
  <%= form.input(
    :occurred_on,
    label: 'Date',
    input_html: { value: params[:date] }
  ) %>
  <%= form.input(:title) %>
  <%= form.submit %>
<% end %>
